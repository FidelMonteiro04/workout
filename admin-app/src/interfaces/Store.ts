export interface Store {
  _id: string;
  owner_id: string;
  image: string;
  name: string;
  description: string;
  address: string;
  lat: string;
  lng: string;
  contact: string;
  instagram?: string;
}
